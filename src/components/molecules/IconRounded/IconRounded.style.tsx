import {StyleSheet} from 'react-native';

export const Style = StyleSheet.create({
  container: {
    borderRadius: 50,
    borderWidth: 2,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
